{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Cron Time",
  "description": "Triggered by a cron expression",
  "type": "object",
  "properties": {
    "service": {
      "type": "string",
      "const": "schedule"
    },
    "event": {
      "type": "string",
      "const": "cron-time"
    },
    "params": {
      "type": "object",
      "properties": {
        "expression": {
          "type": "string"
        },
        "timeZone": {
          "type": "string"
        }
      },
      "required": ["expression", "timeZone"],
      "additionalProperties": false
    }
  },
  "required": ["service", "event", "params"],
  "additionalProperties": false,
  "specs": [
    {
      "id": "SPEC-001",
      "given": "I am working with a cron time",
      "when": "I trigger the automation",
      "then": "it should complete successfully"
    },
    {
      "id": "SPEC-002",
      "given": "entity with service='schedule'",
      "when": "processing configuration",
      "then": "correct handler should be selected"
    },
    {
      "id": "SPEC-003",
      "given": "invalid service value",
      "when": "validating configuration",
      "then": "clear error message should identify the issue"
    },
    {
      "id": "SPEC-004",
      "given": "entity with event='cron-time'",
      "when": "processing configuration",
      "then": "correct handler should be selected"
    },
    {
      "id": "SPEC-005",
      "given": "invalid event value",
      "when": "validating configuration",
      "then": "clear error message should identify the issue"
    },
    {
      "id": "SPEC-006",
      "given": "user configures params",
      "when": "validating input",
      "then": "value should meet schema requirements"
    },
    {
      "id": "SPEC-007",
      "given": "params is set",
      "when": "processing configuration",
      "then": "value should be used correctly"
    },
    {
      "id": "SPEC-008",
      "given": "template with variables",
      "when": "action executes",
      "then": "variables should be substituted with trigger context data"
    },
    {
      "id": "SPEC-009",
      "given": "invalid template syntax",
      "when": "validating automation",
      "then": "error should identify syntax issues"
    },
    {
      "id": "SPEC-010",
      "given": "template variables reference trigger data",
      "when": "automation runs",
      "then": "correct values should be interpolated"
    },
    {
      "id": "SPEC-011",
      "given": "automation with timezone",
      "when": "cron expression evaluates",
      "then": "execution should occur at correct local time"
    },
    {
      "id": "SPEC-012",
      "given": "invalid timezone identifier",
      "when": "validating configuration",
      "then": "error should list valid IANA timezones"
    },
    {
      "id": "SPEC-013",
      "given": "timezone handles DST transitions",
      "when": "schedule crosses DST boundary",
      "then": "execution should occur at correct wall-clock time"
    }
  ],
  "$id": "schedule-cron-time-trigger.schema.json"
}
