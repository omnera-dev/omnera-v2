{
  "$id": "format.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Display format for the result (e.g., currency, percentage)",
  "description": "Display format for the result (e.g., currency, percentage)",
  "type": "string",
  "examples": ["currency", "percentage", "decimal", "date"],
  "x-specs": [
    {
      "id": "APP-FIELD-FORMULA-FORMAT-001",
      "given": "user provides format",
      "when": "validating input",
      "then": "string value should be accepted"
    },
    {
      "id": "APP-FIELD-FORMULA-FORMAT-002",
      "given": "format is empty string",
      "when": "validating input",
      "then": "behavior should follow optional/required rules"
    }
  ]
}
