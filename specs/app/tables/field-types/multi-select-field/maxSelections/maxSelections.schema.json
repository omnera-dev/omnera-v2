{
  "$id": "maxSelections.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Maximum number of selections allowed",
  "description": "Maximum number of selections allowed",
  "type": "integer",
  "minimum": 1,
  "x-specs": [
    {
      "id": "APP-FIELD-MULTI-SELECT-FIELD-MAXSELECTIONS-001",
      "given": "user provides maxSelections >= 1",
      "when": "validating input",
      "then": "value should be accepted"
    },
    {
      "id": "APP-FIELD-MULTI-SELECT-FIELD-MAXSELECTIONS-002",
      "given": "user provides maxSelections < 1",
      "when": "validating input",
      "then": "error should enforce minimum value"
    }
  ]
}
