{
  "$id": "relatedField.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Name of the field in the related table to display",
  "description": "Name of the field in the related table to display",
  "type": "string",
  "minLength": 1,
  "specs": [
    {
      "id": "FIELD-LOOKUP-FIELD-RELATEDFIELD-001",
      "given": "user provides relatedField with at least 1 characters",
      "when": "validating input",
      "then": "value should be accepted"
    },
    {
      "id": "FIELD-LOOKUP-FIELD-RELATEDFIELD-002",
      "given": "user provides relatedField shorter than 1 chars",
      "when": "validating input",
      "then": "error should require minimum length"
    }
  ]
}
