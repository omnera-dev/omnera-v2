{
  "$id": "onDelete.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Action to take when the related record is deleted",
  "description": "Action to take when the related record is deleted",
  "type": "string",
  "default": "restrict",
  "enum": ["cascade", "set-null", "restrict", "no-action"],
  "specs": [
    {
      "id": "APP-FIELD-RELATIONSHIP-FIELD-ONDELETE-001",
      "given": "user selects onDelete from valid options",
      "when": "validating input",
      "then": "selection should be accepted"
    },
    {
      "id": "APP-FIELD-RELATIONSHIP-FIELD-ONDELETE-002",
      "given": "user provides invalid onDelete value",
      "when": "validating input",
      "then": "error should list valid options: cascade, set-null, restrict"
    },
    {
      "id": "APP-FIELD-RELATIONSHIP-FIELD-ONDELETE-003",
      "given": "onDelete is set to any valid enum value",
      "when": "processing entity",
      "then": "appropriate behavior should execute"
    }
  ]
}
